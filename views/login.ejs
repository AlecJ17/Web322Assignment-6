<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection - Login</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
<%- include('partials/navbar', {page: '/login' }) %>

<div class="container mx-auto p-4">
    <div class="max-w-md mx-auto mt-10 bg-white p-8 border border-gray-300 rounded-lg shadow-lg">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center ">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Log In</h1>
                    <p class="py-6">Please enter your Username and Password</p>
                </div>
            </div>
        </div>
        <form method="post" action="/login" class="space-y-6">
            <div class="flex flex-col">
                <label for="userName" class="mb-2 text-sm font-semibold text-gray-700"><strong>User Name</strong></label>
                <input id="userName" name="userName" type="text" placeholder="Enter your username" required
                       class="w-full px-4 py-2 border rounded-md focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50"
                       value="<%= typeof userName != 'undefined' ? userName : '' %>">
            </div>

            <div class="flex flex-col">
                <label for="password" class="mb-2 text-sm font-semibold text-gray-700"><strong>Password</strong></label>
                <input id="password" name="password" type="password" placeholder="Enter your password" required
                       class="w-full px-4 py-2 border rounded-md focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50">
            </div>
            <div class="mt-6 bg-black rounded-lg shadow px-5 py-3">
                <button type="submit"
                        class="w-full text-white font-semibold rounded-lg px-4 py-2 bg-transparent hover:bg-gray-900 focus:bg-gray-900">
                    Log In
                </button>
            </div>
        </form>

        <% if (typeof errorMessage !='undefined' ) { %>
            <div class="mt-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <strong class="font-bold">Error!</strong>
                <span class="block sm:inline"><%= errorMessage %></span>
            </div>
        <% } %>
    </div>
</div>
</body>
</html>
